{
	"name": "AdvisoryBuilder",
	"private": "true",
	"scripts": {
		"build:local": "yarn --cwd frontend install && yarn --cwd frontend build & cargo build",
		"run:local": "yarn run build:local && yarn --cwd frontend run start & cargo run",
		"build:docker": "docker compose build",
		"run:docker": "docker compose up"
	}
}

